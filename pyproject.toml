[project]
name = "smart-routing"
version = "0.1.0"
description = "POC for smart routing between workers"
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = [
    "worker",
]

[tool.uv.sources]
worker = { workspace = true }

[tool.uv.workspace]
members = [
    "packages/*",
]
